---
import type { Translations } from '../i18n/utils';
const { t } = Astro.props as { t: Translations };
---

<section class="py-20 md:py-28 px-6">
  <div class="max-w-7xl mx-auto">
    <p class="eyebrow mb-10">
      {t.overview.eyebrow}
    </p>
    <p class="text-2xl md:text-3xl leading-relaxed text-[var(--color-foreground)] max-w-7xl font-light">
      {t.overview.body}
    </p>

    <figure class="mt-14 md:mt-16">
      <img
        src="/images/photos/team-collab.jpg"
        alt={t.photos.teamOffice}
        width="1408"
        height="768"
        class="w-full h-[260px] md:h-[400px] object-cover rounded-2xl"
        loading="lazy"
        decoding="async"
      />
    </figure>

    <dl class="mt-16 md:mt-20 grid grid-cols-1 md:grid-cols-2 gap-x-16 gap-y-10">
      {t.overview.modules.map((m) => (
        <div>
          <dt class="font-[var(--font-display)] text-2xl md:text-3xl font-semibold tracking-tight text-[var(--color-foreground)]">
            {m.name}
          </dt>
          <dd class="mt-2 text-base md:text-lg text-[var(--color-muted)] leading-relaxed">
            {m.desc}
          </dd>
        </div>
      ))}
    </dl>
  </div>
</section>
